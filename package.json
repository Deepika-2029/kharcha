{
  "name": "gharkharcha-workspace",
  "version": "1.0.0",
  "description": "Family Budget Tracker - Workspace Root",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client",
    "start": "npm run build:client && npm start --prefix server",
    "deploy": "npm run install:all && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
